{
    "$schema": "https://turborepo.org/schema.json",
    "extends": [
        "//"
    ],
    "tasks": {
        "build": {
            "dependsOn": [
                "^build"
            ],
            "outputs": [
                ".hono/**",
                "dist/**",
                ".output/**",
                ".vercel/**"
            ]
        },
        "dev": {
            "persistent": true
        }
    }
}